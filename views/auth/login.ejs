<!------------------------------------------------------------------------------
  login.ejs
------------------------------------------------------------------------------->
<!DOCTYPE html>
<html lang="en-US">

<head>
  <%- include ('../partials/head.ejs') %>
    <link href="/css/login.css" rel="stylesheet">
    </link>
</head>

<body class="text-center">
  <main class="form-signin">
    <form action="postLogin" method="POST">
      <!-- Add csrf token here if needed -->
      <h1 class="famblr-logo">Famblr</h1>
      <h2 class="h3 mb-3 fw-normal text-muted">Please sign in</h1>

        <div class="form-floating">
          <div id="validationServer04Feedback" class="invalid-feedback mb-2">
            Invalid Email or Password
          </div>
          <input type="email" name="email" value="<%= oldInput.email %>"
            class="form-control <%= errorMessage ? 'is-invalid' : '' %>"
            id="floatingInput" placeholder="name@example.com" required>
          <label for="floatingInput">Email address</label>
        </div>

        <div class="form-floating">
          <input type="password" name="password"
            class="form-control <%= errorMessage ? 'is-invalid' : '' %>"
            id="floatingPassword" placeholder="Password" required>
          <label for="floatingPassword">Password</label>
           
          <div id="validationServer04Feedback" class="invalid-feedback mb-2">
            <%= errorMessage %>
          </div>
        </div>

        <button class="mt-3 w-100 btn btn-lg btn-primary" type="submit">Sign in</button>

        <p class="mt-3">Dont have an account? <a href="/account/signup">Sign Up</a></p>
        <p class="mt-5 mb-3 text-muted">© 2021</p>
    </form>
  </main>
</body>

<!-- <body>
    <h1>Famblr</h1> 
    <form action="postLogin" method="POST">
        <div class="container">
          <label for="email"><b>Email</b></label>
          <input type="text" 
            class="<%= validationErrors.find(e => e.param === 'email') ? 'invalid' : '' %>"
            placeholder="Enter Email" 
            name="email" 
            value="<%= oldInput.email %>"
            required
          >
          <br>
          <label for="password"><b>Password</b></label>
          <input type="password" 
            class="<%= validationErrors.find(e => e.param === 'password') ? 'invalid' : '' %>"
            placeholder="Enter Password" 
            name="password" 
            required
          >
          <br>
          
          <button type="submit">Log in</button>
        </div>
      </form> 
</body> -->